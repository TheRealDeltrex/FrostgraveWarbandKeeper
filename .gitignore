__pycache__/
*.py[cod]
.venv/
venv/
.env
data/uploads/*
!data/uploads/.gitkeep
data/warbands/*
!data/warbands/.gitkeep
data/portraits/*
!data/portraits/.gitkeep
*.egg-info/
.DS_Store
build/
dist/
# The packaged release archive is built beside dist/ and attached to the
# GitHub release, never committed — it is ~46 MB and would live in history
# forever after a single `git add -A`.
FrostgraveWarbandKeeper-*.zip
.codesign/

# Local AI-agent operating manual (CLAUDE.md) and its pointer for other tools.
# Machine-specific (absolute paths, signing setup), deliberately not published.
CLAUDE.md
AGENTS.md

# docs/app/index.html and docs/index.html are hand-authored source (tracked
# below); everything else under docs/ is generated by build_browser_bundle.py
# / build_preview_pages.py and rebuilt fresh by the Pages deploy workflow.
docs/app/bundle.json
docs/app/static/
docs/static/
docs/preview-*.html
